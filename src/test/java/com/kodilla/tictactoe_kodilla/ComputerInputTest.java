package com.kodilla.tictactoe_kodilla;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

class ComputerInputTest {
/*
    @Test
    void shouldReturnCoordinatesFor11() throws OutOfAttemptsException {
        //Given
        ComputerInput input = new ComputerInput();
        when().thenReturn("11");

        //When
        Coordinates actual = input.computerPicksPosition();

        //Then
        assertEquals(0, actual.getRow());
        assertEquals(0, actual.getColumn());
    }

    @Test
    void shouldReturnCoordinatesFor33() throws OutOfAttemptsException {
        //Given
        ComputerInput input = new ComputerInput();
        when().thenReturn("33");

        //When
        Coordinates actual = input.computerPicksPosition();

        //Then
        assertEquals(2, actual.getRow());
        assertEquals(2, actual.getColumn());

    }

    @Test
    void shouldThrowOutOfAttemptsException() {

        //CustomType mockCustomType = Mockito.mock(CustomType.class);  // Replace "CustomType" with the actual custom type
        // Mockito.when(yourClass.convertStringToObject(Mockito.anyString())).thenReturn(mockCustomType);

        // Test with known input values
        // int[] inputValues = {0, 1, 2};
        //  for (int inputValue : inputValues) {
        // Call the method with the random input
        //    CustomType result = yourClass.yourMethod(inputValue);

        // Verify that the conversion method was called with the expected string
        //   Mockito.verify(yourClass).convertStringToObject(Integer.toString(inputValue));

        // Perform assertions on the result
        // For example, you can use Mockito's assertions or compare specific properties of the custom type
        // Mockito.verify(mockCustomType).someMethod();

        //Given
//        ComputerInput input = new ComputerInput();
//        Coordinates mockMove = Mockito.mock(Coordinates.class);

        //    when(mockMove.getRow()).thenReturn("00");


        //When
        // Test with known input values
        // int[] inputValues = {0, 1, 2};
        //  for (int inputValue : inputValues) {
        // Call the method with the random input
        //    CustomType result = yourClass.yourMethod(inputValue);
        //Then
        //  Assertions.assertThrowsExactly(OutOfAttemptsException.class, input::getMove);
        //  verify(mockScanner, times(5)).nextLine();
//
//        Mockito.verify(mockMove).getColumn();
//        Mockito.verify(mockMove).getRow();
//        Mockito.reset(input, mockMove);
    }

 */
}